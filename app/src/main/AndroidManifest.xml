<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.onlinequizapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".BroadCastReceiver.AlarmReceiver" />

        <activity android:name=".Activities.Home" />
        <activity android:name=".Activities.Start" />
        <activity android:name=".Activities.Playing" />
        <activity android:name=".Activities.Done" />
        <activity android:name=".Activities.ScoreDetail" />
        <activity android:name=".Activities.newQuestion"></activity>
    </application>

</manifest>